{% extends 'base.html.twig' %}

{% block title %}Nouveau Ticket {% endblock %}

{% block body %}
    <h1>Créer un nouveau ticket (Membre du personnel)</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}

    <a href="{{ path('app_ticket_user') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
{% endblock %}